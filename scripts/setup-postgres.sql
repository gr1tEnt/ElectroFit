-- Run in pgAdmin (Query Tool) while connected as a superuser.
-- Use the same password as SPRING_DATASOURCE_PASSWORD in your .env file.

-- ALTER USER postgres WITH PASSWORD 'your-password-from-env';

CREATE DATABASE electrofit_db;
