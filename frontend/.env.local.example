# Copy to `.env.local` for local Next.js dev (this file is safe to commit).
NEXT_PUBLIC_API_URL=http://localhost:8080
# Production example: https://electrofit-backend.onrender.com
# Use the backend origin only — no trailing slash, no /api suffix.
